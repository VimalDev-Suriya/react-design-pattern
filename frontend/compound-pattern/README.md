# React - Compound components Design pattern

We should use this pattern, when we need a flexible UI componets hthat usually works in the more coupled way to its parent.

We can use this pattern, when we should give the maximum control over the structure and composition of components.
