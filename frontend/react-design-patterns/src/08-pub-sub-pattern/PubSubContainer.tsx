import AddToCartButton from './components/publishers/AddToCartButton';
import CartBadge from './components/subscribers/CartBadge';

const PubSubContainer = () => {
  return (
    <div>
      <AddToCartButton />
      <CartBadge />
    </div>
  );
};

export default PubSubContainer;
