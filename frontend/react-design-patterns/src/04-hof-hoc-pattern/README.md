# Higher Order Components and Functions:

Higher order functions (HOF) - a function that takes a function as the arguments or return the new function or do both are called as HOF

- We will use "with" prefix for all type of HOF
- It accepts the components and return the enhanced component
